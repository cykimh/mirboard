# 테스트 전역 기본값.
# D-84 — 통합테스트는 같은 IP(127.0.0.1)에서 register/login 을 다수 호출하므로
# 인증 IP 레이트리밋을 사실상 비활성화한다(프로덕션 기본값은 application.yml 의 20/분 유지).
# 레이트리밋 자체를 검증하는 테스트(AuthAbuseIntegrationTest)는 @TestPropertySource 로
# limit 을 낮게 명시해 이 값을 override 한다.
mirboard.ratelimit.auth.limit=1000000
