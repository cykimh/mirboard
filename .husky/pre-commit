#!/usr/bin/env bash
# Mirboard pre-commit hook (Phase 11 — D-60).
#
# `scripts/check.sh fast` 로 위임 — 검증 로직 single source.
# 활성화: `git config core.hooksPath .husky` (한 번만).
# 우회: `git commit --no-verify`.

exec "$(git rev-parse --show-toplevel)/scripts/check.sh" fast
